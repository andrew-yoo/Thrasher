[project]
name = "bulwark"
version = "0.2.0"
description = "A lightweight file encryption program"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=49.0.0",
    "pynacl>=1.6.2",
]
